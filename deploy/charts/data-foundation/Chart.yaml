apiVersion: v2
name: data-foundation
description: |
  采集/计算（占位子 chart）。由 umbrella chart `platform` 按 `data-foundation.enabled` 条件聚合。
  实际部署模板由对应 submodule services/data-foundation 贡献后逐步充实（schema-first / 占位先行）。
type: application

# 占位版本：尚未发布。
version: 0.0.0-placeholder
appVersion: "0.0.0"

keywords:
  - hashmatrix
  - data-foundation
  - Java/Flink

home: https://github.com/HashMatrixData/hashmatrix-data-foundation
sources:
  - https://github.com/HashMatrixData/hashmatrix-data-foundation

apiVersion: v2
name: control-plane
description: |
  租户注册/开通/生命周期/配额（控制平面）（占位子 chart）。由 umbrella chart `platform` 按 `control-plane.enabled` 条件聚合。
  实际部署模板由对应 submodule services/control-plane 贡献后逐步充实（schema-first / 占位先行）。
type: application

# 占位版本：尚未发布。
version: 0.0.0-placeholder
appVersion: "0.0.0"

keywords:
  - hashmatrix
  - control-plane
  - Java

home: https://github.com/HashMatrixData/hashmatrix-control-plane
sources:
  - https://github.com/HashMatrixData/hashmatrix-control-plane

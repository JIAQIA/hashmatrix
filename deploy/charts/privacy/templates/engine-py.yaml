apiVersion: apps/v1
kind: Deployment
metadata:
  # 固定 Service/工作负载名（对齐 governance 约定）；编排层经此名访问引擎，umbrella 下亦稳定。
  name: privacy-engine-py
  labels:
    {{- include "privacy.labels" . | nindent 4 }}
    app.kubernetes.io/component: engine-py
spec:
  replicas: {{ .Values.enginePy.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/component: engine-py
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: engine-py
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: engine-py
          image: {{ include "privacy.enginePy.image" . | quote }}
          imagePullPolicy: {{ .Values.enginePy.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.enginePy.containerPort }}
          env:
            # 绑定端口显式注入（与 containerPort 一致，避免镜像内默认漂移）
            - name: ENGINE_PORT
              value: {{ .Values.enginePy.containerPort | quote }}
            {{- range $k, $v := .Values.enginePy.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.enginePy.containerPort }}
          resources:
            {{- toYaml .Values.enginePy.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  # 固定 Service/工作负载名（对齐 governance 约定）；编排层经此名访问引擎，umbrella 下亦稳定。
  name: privacy-engine-py
  labels:
    {{- include "privacy.labels" . | nindent 4 }}
    app.kubernetes.io/component: engine-py
spec:
  selector:
    app.kubernetes.io/component: engine-py
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - port: {{ .Values.enginePy.containerPort }}
      targetPort: {{ .Values.enginePy.containerPort }}

apiVersion: v2
name: governance
description: |
  数据治理分系统（占位子 chart）。由 umbrella chart `platform` 按 `governance.enabled` 条件聚合。
  实际部署模板由对应 submodule services/governance 贡献后逐步充实（schema-first / 占位先行）。
type: application

# 占位版本：尚未发布。
version: 0.0.0-placeholder
appVersion: "0.0.0"

keywords:
  - hashmatrix
  - governance
  - Java

home: https://github.com/HashMatrixData/hashmatrix-governance
sources:
  - https://github.com/HashMatrixData/hashmatrix-governance

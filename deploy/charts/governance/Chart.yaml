apiVersion: v2
name: governance
description: |
  数据治理分系统子 chart（M1 真实部署）。由 umbrella chart `platform` 按 `governance.enabled` 聚合。
  D5：子仓 services/governance 交付 image，主仓 owns chart。M1 提供 Deployment + Service（应用 8082 /
  管理 9082），datasource 指向 infra-dev 的 in-cluster PG；`/api/meta/search` 返回脱敏 mock（#4），
  引擎核心/实例/采集属 post-M1。
type: application

version: 0.1.0
appVersion: "0.1.0-SNAPSHOT"

keywords:
  - hashmatrix
  - governance
  - Java

home: https://github.com/HashMatrixData/hashmatrix-governance
sources:
  - https://github.com/HashMatrixData/hashmatrix-governance

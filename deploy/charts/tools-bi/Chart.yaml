apiVersion: v2
name: tools-bi
description: |
  报表/可视编排（占位子 chart）。由 umbrella chart `platform` 按 `tools-bi.enabled` 条件聚合。
  实际部署模板由对应 submodule services/tools-bi 贡献后逐步充实（schema-first / 占位先行）。
type: application

# 占位版本：尚未发布。
version: 0.0.0-placeholder
appVersion: "0.0.0"

keywords:
  - hashmatrix
  - tools-bi
  - Java/TS

home: https://github.com/HashMatrixData/hashmatrix-tools-bi
sources:
  - https://github.com/HashMatrixData/hashmatrix-tools-bi
